# CLEAN1 CI inputs

Upload only the two authenticated ZIP inputs required by `BB_CLEAN1_WINDOWS_BUILD_CONTRACT_V1.md` into this directory, preserving the exact filenames and bytes.

Required filenames:
- `BB_BLACKBOX_V1_CI_CLEAN1_SOURCE_READY4_2026-09-15.zip`
- `BB_BLACKBOX_V1_CI_CLEAN1_SOURCE_READY4_PATCHFIX1_2026-09-15.zip`

The contract workflow rejects any hash mismatch before extraction.
